---
title: API Access
description: Access an HTTP endpoint and retrieve external information.
---

# 🌐 Node: API Access

This node allows you to connect to an external web service (API) and fetch data for use within the flow.

## 🧩 Function
Makes an HTTP request (GET, POST, etc.) to a URL and stores the response.

## ⚙️ Configuration

| Field           | Type    | Description                                       |
|-----------------|---------|---------------------------------------------------|
| url             | Text    | URL of the API service                            |
| method          | Select  | HTTP verb: GET, POST, PUT, DELETE, etc.           |
| credentials_id  | Text    | Credential ID for authorization (optional)        |

## 📝 Common Usage
- Query product catalogs  
- Retrieve customer data from external services  
- Validate tokens or responses in real time  

## 🌍 Multi-language Support
- `help.es`: Accede a un endpoint y obtiene la información asignándola a un campo data  
- `help.en`: Access an endpoint and retrieve information by assigning it to a data field  
